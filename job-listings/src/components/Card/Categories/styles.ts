import { StyleSheet } from "react-native";

export default StyleSheet.create({
    categories: {
        flexDirection: 'row', 
        flexWrap: 'wrap', 
        columnGap: 20,
        rowGap: 16,
        marginVertical: 8,
        paddingTop: 20,
        borderTopWidth: 0.9,
        borderColor: 'hsl(180, 8%, 52%)'
    }
});